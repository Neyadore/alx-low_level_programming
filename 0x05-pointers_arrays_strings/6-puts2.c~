#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 *puts2 - prints every otther hcaracter of a string
 *@str: the string
 *Return: returns nothing
 */

void puts2(char *str)
{
	int length = strlen(str);

	for (i = 0; i < lenght; i++)
	{
		if (length % 2 == 0)
		{
			_putchar(str[i]);
		}
	}
	_putchar('\n');
}
