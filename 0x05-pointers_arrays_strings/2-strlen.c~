#include "main.h"
#include <stdlib.h>

/**
 * _strlen - returns length of a string
 * @s: string one
 *Return: returns nothing
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; *s[i] != '\0'; )
		i++;
}
